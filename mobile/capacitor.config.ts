import type { CapacitorConfig } from '@capacitor/cli';
const config: CapacitorConfig = { appId: 'com.visionassist.app', appName: 'VisionAssist', webDir: 'www/browser', server: { androidScheme: 'https' } };
export default config;
