import { CommonModule } from '@angular/common'; import { Component,OnInit } from '@angular/core'; import { ActivatedRoute,Router,RouterLink } from '@angular/router'; import { IonButton,IonContent } from '@ionic/angular/standalone'; import type { VisionAnalysis } from '../../core/models/vision.models'; import { VisionApiService } from '../../core/services/vision-api.service';
@Component({
    selector: 'app-result', imports: [CommonModule, IonContent, IonButton, RouterLink], templateUrl: './result.page.html', styleUrl: './result.page.scss'
}) export class ResultPage implements OnInit{result?:VisionAnalysis;constructor(private route:ActivatedRoute,private router:Router,private api:VisionApiService){}ngOnInit(){const state=history.state as {result?:VisionAnalysis};if(state.result){this.result=state.result;return}const id=this.route.snapshot.paramMap.get('id')!;this.api.detail(id).subscribe({next:r=>this.result=r.result,error:()=>void this.router.navigate(['/scanner'])})}}
